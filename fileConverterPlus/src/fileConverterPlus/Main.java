package fileConverterPlus;

import java.awt.Color;

import javax.swing.ImageIcon;
import javax.swing.JFrame;

public class Main {

	public static void main(String[] args) {
		
		/*
		 * JFrame frame = new JFrame(); frame.setTitle("Title here");
		 * frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
		 * //frame.setResizable(false); frame.setSize(500,500); frame.setVisible(true);
		 * 
		 * ImageIcon image = new ImageIcon("ruler.png");
		 * frame.setIconImage(image.getImage());
		 * frame.getContentPane().setBackground(new Color(93, 93, 93));
		 */
		
		EntryFrame frame = new EntryFrame();
	}

}
